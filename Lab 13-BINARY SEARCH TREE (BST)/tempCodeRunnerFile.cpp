  // bool found;
    // myTree.Search(20, found);
    // cout << (found ? "Item is found" : "Item is not found") << endl;
    // myTree.Search(15, found);
    // cout << (found ? "Item is found" : "Item is not found") << endl;